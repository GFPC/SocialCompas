{
  "version": 2,
  "buildCommand": "npm --prefix miniapp install && npm --prefix miniapp run build",
  "outputDirectory": "miniapp/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
